server:
  port: 8081

spring:
  application:
    name: memberservice

    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://172.16.211.43:3306/KADB?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
      username: root
      password: qwer1234!

    jpa:
      show-sql: true

      hibernate:
        ddl-auto: update

      properties:
        hibernate:
          format_sql: true

eureka:
  instance:
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 30

  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/